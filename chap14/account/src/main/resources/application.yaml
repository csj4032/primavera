spring:
  application:
    name: account
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888

server:
  port: 8081
  tomcat:
    max-threads: 1

logging:
  level:
    org.springframework: DEBUG