spring:
  config:
    activate:
      on-profile: test
    import:
      - "optional:vault:"
      - "optional:configserver:"
  cloud:
    config:
      enabled: false
    vault:
      enabled: false
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  elasticsearch:
    uris: http://localhost:9200

primavera:
  testcontainers:
    elasticsearch:
      enabled: true
      dockerImageName: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
      username: 
      password: 

debezium:
  enabled: false

logging:
  level:
    com.genius.primavera: DEBUG
    org.springframework.data.elasticsearch: DEBUG
    co.elastic.clients: INFO
